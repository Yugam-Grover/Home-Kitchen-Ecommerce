export default function LoginPage(): React.ReactElement {
    return (
        <main className="container-narrow" style={{ paddingBlock: '96px', maxWidth: '480px', marginInline: 'auto' }}>
            <h1 className="text-heading-lg" style={{ textAlign: 'center' }}>Sign In</h1>
            <p className="text-body-md" style={{ color: 'var(--color-text-secondary)', marginTop: '8px', textAlign: 'center' }}>Login form — Phase 3</p>
        </main>
    );
}

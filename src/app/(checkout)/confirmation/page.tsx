export default function ConfirmationPage(): React.ReactElement {
    return (
        <main className="container-narrow" style={{ paddingBlock: '64px', textAlign: 'center' }}>
            <h1 className="text-heading-lg">Order Confirmed</h1>
            <p className="text-body-md" style={{ color: 'var(--color-text-secondary)', marginTop: '8px' }}>
                Confirmation page — Phase 3
            </p>
        </main>
    );
}
